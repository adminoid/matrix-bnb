
Two kinds of event count:
`WhoseRegistered`
`DirectTransfer`

**WhoseRegistered** happens:
1. when the first five registered
2. register(whose)
3. matricesRegistration -> if user is not registered in matrix level

**DirectTransfer** happens:
1. receive(msg.sender, msg.value)

```
Я кажется нашел проблему.

Там для общей суммы считаются 2 события, одно называется DirectTransfer и оно срабатывает когда на контракт кто-то посылает деньги
Другое событие WhoseRegistered срабатывает:
1) Когда первые 5 кошельков регистрируются следующий под предыдущим
2) когда кто-то запускает метод register (под кем-то либо 0 кошелек по умолчанию
3) когда кто-то переводит прямой перевод на контракт и этим он регистрируется

Первое событие DirectTransfer срабатывает когда кто-то сколько угодно переводит на контракт, затем второе событие может сработать если этим он регистрируется в системе, тем самым вот именно здесь может быть наложение сумм.

Нужно подумать, что конкретно надо считать для общей суммы и как это реализовать. Потому что события срабатывают правильно, одно при регистрации пользователя, второе при прямом переводе на контракт.
```